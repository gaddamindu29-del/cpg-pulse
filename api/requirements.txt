# Slim, API-only dependency set for the Docker image (docker-compose.yml's
# `api` service) -- distinct from the top-level requirements.txt, which also
# includes pyspark/airflow/dbt and would bloat this image for no benefit.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
SQLAlchemy==2.0.31
psycopg2-binary==2.9.9
