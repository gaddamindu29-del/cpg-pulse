# --- Phase 2: synthetic data generator ---
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# --- Phase 4: ingestion & PySpark standardization ---
pyspark==3.5.1
boto3==1.34.131
PyYAML==6.0.1

# --- Phase 5/6: warehouse + dbt ---
dbt-core==1.8.3
dbt-postgres==1.8.2
dbt-snowflake==1.8.3
psycopg2-binary==2.9.9
SQLAlchemy==2.0.31

# --- Phase 7: API + dashboard ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
streamlit==1.36.0
plotly==5.22.0
requests==2.32.3
python-dotenv==1.0.1

# --- Orchestration ---
# apache-airflow is deliberately NOT installed here -- it's fundamentally
# incompatible with SQLAlchemy==2.0.31 above (Airflow 2.9.2 requires
# sqlalchemy<2.0,>=1.4.36, an unresolvable pip conflict when both are pinned
# in the same environment). Nothing outside airflow/ imports the `airflow`
# package; Airflow only ever runs inside its own Docker image, which has its
# own separate airflow/requirements-airflow.txt built against Airflow's
# official --constraint file instead of arbitrary pins. See docs/runbook.md.

# --- Testing & quality ---
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0
